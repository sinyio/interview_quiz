import { Button } from '@/shared/ui/Button';
import { Text } from '@/shared/ui/Text';

export const LoginButton = () => {
  return (
    <Button variant="link-purple">
      <Text variant="body4-accent" color="purple-700">
        Вход
      </Text>
    </Button>
  );
};
